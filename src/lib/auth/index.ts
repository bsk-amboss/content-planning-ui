// No auth wired yet. Wire your identity provider here and export a
// getCurrentUser() that returns the session user (or null).
export async function getCurrentUser(): Promise<null> {
  return null;
}
