// No database wired yet. When adding persistence, declare the client here
// (Drizzle / Prisma / Kysely / raw driver) and export it as `db` so server
// code imports from one place: `import { db } from '@/lib/db';`.
export const db = null;
